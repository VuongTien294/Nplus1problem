server:
  port: 8080

spring:
  application:
    name: test
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/test?useSSL=false&useUnicode=true&characterEncoding=UTF-8}
    username: postgres
    password: myPassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
